# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# Environment — never commit the real key
backend/.env

# Node
frontend/node_modules/

# Keep frontend/dist (Railway needs it pre-built)
# Keep backend/uploads/axiom.db (seeds demo data on first run anyway)
